export const CELL_METERS = 0.0035;
export const TEXELS_PER_CELL = 3;
export const GRAVITY = 9.81;
export const PHYSICS_STEP = 1 / 120;
export const MAX_PHYSICS_STEPS = 4;

export const SPECIMEN = Object.freeze({
  targetArea: 0.3,
  maxSide: 1.05,
  minCells: 8,
  alphaThreshold: 128,
  dropHeight: 0.05,
  minIslandCells: 40,
});

export const VIEW = Object.freeze({
  frameAspect: 16 / 9,
  floorLine: 0.84,
  subjectHeightShare: 0.5,
  subjectWidthShare: 0.62,
});

export const HAMMER = Object.freeze({
  length: 0.54,
  headLength: 0.18,
  headWidth: 0.058,
  handleWidth: 0.045,
  restLift: 0.45,
  windUpLift: (100 * Math.PI) / 180,
  fullLift: (160 * Math.PI) / 180,
  raiseSeconds: 0.09,
  swingSeconds: [0.08, 0.3],
  swingWeight: [2, 4],
  recoilSeconds: 0.2,
  tremble: 0.04,
});

export const CHARGE = Object.freeze({
  seconds: 3.6,
  tiers: [0.2, 0.4, 0.6, 0.8, 1],
  minRadius: 0.03,
  maxRadius: 0.24,
  minStrength: 1,
  maxStrength: 4.5,
  hits: 12,
  falloff: 0.5,
  blastReach: 1.6,
  blastSpeed: 5,
  blastHeft: 0.35,
  burst: 1.8,
  spray: 2.5,
});

export const BOMB = Object.freeze({
  size: 0.03,
  fuseSeconds: 2,
  capacity: 6,
  blinkRate: [2, 14],
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.17,
    strength: 3.2,
    hits: 9,
    falloff: 0.6,
    shatter: true,
    spray: 2.2,
    burst: 2.6,
    blast: Object.freeze({ reach: 0.5, speed: 7, heft: 0.35 }),
    force: 0.85,
    cue: 'explode',
  }),
});

export const SAW = Object.freeze({
  radius: 0.11,
  spin: 14,
  spool: 12,
  biteSeconds: 1 / 15,
  whirSeconds: 0.16,
  spray: 1.4,
  burst: 2.4,
  drive: 1,
  shove: 1.5,
  heft: 0.3,
  shake: 0.003,
  recoil: Object.freeze({ speed: 0.25, climb: 0.5, stiffness: 400, damping: 18 }),
  shock: Object.freeze({ trauma: 0.32, kick: 0.06, punch: 0.004, flash: 0 }),
});

export const KATANA = Object.freeze({
  delay: 0.9,
  pause: 0.35,
  tempo: 5,
  cadence: 0.08,
  steadyCuts: 3,
  quickening: 0.97,
  drawIn: 0.07,
  afterglow: 0.35,
  minLength: 0.04,
  width: 0.012,
  part: 1,
  lift: 0.4,
  spin: 2,
  tension: 0.5,
  sparkles: 6,
  sparkleReach: 0.01,
  slashShock: Object.freeze({ trauma: 0.15, kick: 0.1, punch: 0.01, flash: 0.15 }),
  severShock: Object.freeze({ trauma: 0.6, kick: 0.4, punch: 0.05, flash: 0.45 }),
});

export const GANTRY = Object.freeze({
  restGap: 0.02,
  travel: 2.5,
  overhang: 0.2,
});

export const PRESS = Object.freeze({
  halfWidth: 0.17,
  plateHeight: 0.06,
  rodWidth: 0.07,
  floorGap: 0.004,
  contactGap: 0.006,
  band: 0.02,
  eject: 0.6,
  drop: 0.3,
  speed: 0.25,
  lift: 1.2,
  tempo: 4,
  crushSeconds: 1 / 20,
  humSeconds: 0.2,
  cracks: 4,
  crackReach: 0.012,
  bins: 32,
  push: 0.15,
  slide: 0.12,
  spread: 2.5,
  squashStep: 0.01,
  squashShare: 0.6,
  minSquash: 0.1,
  burst: 1.4,
  squeeze: 0.25,
  tension: Object.freeze({ moving: 0.2, crushing: 0.7 }),
  shock: Object.freeze({ trauma: 0.4, kick: 0.08, punch: 0.006, flash: 0 }),
  landShock: Object.freeze({ trauma: 0.8, kick: 0.6, punch: 0.04, flash: 0.1 }),
});

export const BALL = Object.freeze({
  radius: 0.085,
  surface: Object.freeze({ density: 900, friction: 0.5, restitution: 0.12 }),
  reloadSeconds: 1,
  searSeconds: 1 / 15,
  sizzleSeconds: 0.14,
  crackSeconds: 0.35,
  charReach: 1.3,
  melt: 1,
  smoke: 1,
  tension: 0.3,
  landSpeed: 1.2,
  landShock: Object.freeze({ trauma: 0.55, kick: 0.45, punch: 0.02, flash: 0 }),
  restSpeed: 0.05,
  restSeconds: 1.2,
  lifeSeconds: 15,
  fadeSeconds: 0.6,
});

export const DRILL = Object.freeze({
  radius: 0.012,
  feed: 0.07,
  depth: 0.11,
  retract: 0.5,
  spin: 60,
  spool: 8,
  biteSeconds: 1 / 20,
  whineSeconds: 0.14,
  crackSeconds: 0.4,
  crack: [0.25, 1.1],
  pressureSeconds: 1.6,
  spray: 0.6,
  burst: 0.6,
  shake: 0.0025,
  tension: 0.25,
  shock: Object.freeze({ trauma: 0.28, kick: 0.02, punch: 0.002, flash: 0 }),
});

export const GUN = Object.freeze({
  rate: 14,
  pellets: 1,
  spread: [0.004, 0.035],
  bloomSeconds: 1.2,
  recovery: 2.5,
  flashSeconds: 0.07,
  round: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.014,
    strength: 0.8,
    hits: 1,
    falloff: 0,
    shatter: false,
    spray: 0.7,
    burst: 0.9,
    contact: 0.004,
    blast: Object.freeze({ reach: 0.06, speed: 1.4, heft: 0.2 }),
    cue: 'gunshot',
    shock: Object.freeze({ trauma: 0.14, kick: 0.05, punch: 0.004, flash: 0.03 }),
  }),
});

export const LASER = Object.freeze({
  radius: 0.004,
  mount: 0.06,
  searSeconds: 1 / 30,
  humSeconds: 0.12,
  charReach: 2.2,
  melt: 0.3,
  smoke: 0.4,
  glance: 1,
  crackSeconds: 0.5,
  tension: 0.2,
});

export const LIGHTNING = Object.freeze({
  leaderSeconds: 0.12,
  boltSeconds: 0.4,
  drift: 0.25,
  jag: 0.28,
  depth: 4,
  branches: 3,
  branchReach: [0.12, 0.3],
  charReach: 0.014,
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.07,
    strength: 2.6,
    hits: 6,
    falloff: 0.5,
    shatter: false,
    spray: 1.6,
    burst: 1.8,
    contact: 0.01,
    blast: Object.freeze({ reach: 0.3, speed: 3.5, heft: 0.3 }),
    force: 0.65,
    cue: 'thunder',
  }),
});

export const METEOR = Object.freeze({
  capacity: 4,
  speed: 4.2,
  slant: [0.35, 0.8],
  reach: 0.03,
  size: 0.034,
  smoke: 1,
  blastSeconds: 0.6,
  embers: 40,
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.2,
    strength: 3.8,
    hits: 10,
    falloff: 0.55,
    shatter: true,
    spray: 2.4,
    burst: 2.6,
    contact: 0.035,
    blast: Object.freeze({ reach: 0.7, speed: 8, heft: 0.35 }),
    force: 1,
    cue: 'impact',
  }),
});

export const ROCKET = Object.freeze({
  capacity: 3,
  launchSpeed: 0.8,
  maxSpeed: 5.5,
  thrust: 9,
  wobble: 0.45,
  wobbleRate: 11,
  settle: 2.5,
  height: 0.07,
  inset: 0.04,
  reach: 0.012,
  length: 0.075,
  width: 0.017,
  smoke: 1,
  blastSeconds: 0.45,
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.14,
    strength: 3,
    hits: 8,
    falloff: 0.6,
    shatter: true,
    spray: 2,
    burst: 2.4,
    contact: 0.015,
    blast: Object.freeze({ reach: 0.45, speed: 6.5, heft: 0.35 }),
    force: 0.8,
    cue: 'explode',
  }),
});

export const NUKE = Object.freeze({
  capacity: 1,
  drop: 0.3,
  gravity: 2.4,
  reach: 0.035,
  size: 0.05,
  blastSeconds: 2.4,
  embers: 120,
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.34,
    strength: 6,
    hits: 16,
    falloff: 0.4,
    shatter: true,
    spray: 3,
    burst: 3.2,
    contact: 0.05,
    blast: Object.freeze({ reach: 3, speed: 13, heft: 0.5 }),
    force: 1,
    cue: 'nuke',
  }),
});

export const AXE = Object.freeze({
  capacity: 3,
  flightSeconds: 0.42,
  spin: 17,
  reach: 0.018,
  inset: 0.05,
  height: 0.28,
  depth: 0.08,
  strength: 1.6,
  size: 0.02,
  embed: 0.019,
  stickSeconds: 2.5,
  fadeSeconds: 0.4,
  shock: Object.freeze({ trauma: 0.4, kick: 0.25, punch: 0.015, flash: 0 }),
});

export const BLACKHOLE = Object.freeze({
  growSeconds: 2.5,
  follow: 6,
  horizon: [0.012, 0.045],
  reach: [0.25, 0.6],
  pull: [5, 16],
  swirl: 0.55,
  heft: 0.4,
  feedSeconds: 1 / 15,
  humSeconds: 0.15,
  flareSeconds: 0.35,
  tension: [0.2, 0.7],
  collapse: Object.freeze({
    normalX: 0,
    normalY: 1,
    hits: 8,
    falloff: 0.5,
    shatter: false,
    spray: 2,
    burst: 2.8,
    contact: 0.01,
    cue: 'collapse',
  }),
  collapseRadius: 3,
  collapseStrength: [1.2, 4],
  collapseSpeed: [3, 10],
});

export const TORNADO = Object.freeze({
  spinUp: 0.7,
  travel: 1.1,
  radius: [0.08, 0.3],
  topShare: 0.85,
  lift: 30,
  inward: 6,
  sway: 2.5,
  fling: 8,
  flingShare: 0.55,
  heft: 0.6,
  dustSeconds: 0.07,
  galeSeconds: 0.15,
  tension: 0.3,
});

export const QUAKE = Object.freeze({
  rampSeconds: 2.5,
  pulseSeconds: [0.24, 0.1],
  power: [0.25, 1],
  reach: 0.25,
  kick: 3,
  sideways: 0.5,
  spin: 2.5,
  heft: 0.3,
  puffs: 3,
  tremors: 4,
  crack: 0.9,
  grip: 0.02,
  spray: 0.6,
  rumbleSeconds: 0.15,
  trauma: [0.3, 0.8],
  tension: 0.35,
});

export const LEVITY = Object.freeze({
  lift: 1.3,
  ceilingShare: 0.75,
  band: 0.12,
  drag: 3,
  spin: 0.8,
  heft: 1,
  slam: 7,
  humSeconds: 0.15,
});

export const GRAB = Object.freeze({
  reach: 0.04,
  stiffness: 16,
  gain: 14,
  maxSpeed: 7,
  spinDamping: 3,
  tension: 0.15,
});

export const FREEZE = Object.freeze({
  radius: 0.05,
  chillSeconds: 1 / 15,
  coldSeconds: 2.2,
  brittle: 0.05,
  ice: '205,236,255',
  frost: 0.12,
  mist: 3,
  crackChance: 0.25,
  crack: 0.35,
  hissSeconds: 0.14,
  tension: [0.1, 0.45],
  burst: Object.freeze({
    normalX: 0,
    normalY: 1,
    falloff: 0.4,
    spray: 1.5,
    contact: 0.01,
    cue: 'frostburst',
  }),
  strength: [0.5, 3.4],
  hits: [2, 10],
  spread: [0.5, 2],
  speed: [1, 4],
  shatterAt: 0.6,
});

export const ACID = Object.freeze({
  capacity: 14,
  size: 0.011,
  biteSeconds: 0.1,
  bite: 0.013,
  cost: 0.035,
  dripChance: 0.08,
  dripShare: 0.55,
  dripMinimum: 0.3,
  fumes: 2,
  stain: '96,128,24',
  corrosion: 0.35,
  bubbleSeconds: 0.18,
  fadeSeconds: 0.5,
});

export const TERMITE = Object.freeze({
  colony: 10,
  capacity: 40,
  speed: 0.05,
  sense: 0.05,
  bite: 0.006,
  biteSeconds: 0.14,
  turn: 5,
  lifeSeconds: 7,
  fadeSeconds: 0.5,
  scatter: 0.02,
  size: 0.008,
  crumbs: 1,
});

export const TESLA = Object.freeze({
  reach: 0.22,
  arcs: 3,
  zapSeconds: 0.07,
  strength: 0.45,
  jolt: 0.6,
  char: 0.009,
  sparks: 1,
  buzzSeconds: 0.12,
  tension: 0.25,
});

export const ORBITAL = Object.freeze({
  capacity: 2,
  lockSeconds: 0.9,
  fireSeconds: 0.45,
  fadeSeconds: 0.35,
  halfWidth: 0.035,
  char: 0.35,
  embers: 6,
  shock: Object.freeze({ trauma: 0.9, kick: 0.5, punch: 0.04, flash: 0.5 }),
  blast: Object.freeze({ reach: 0.35, speed: 5, heft: 0.4 }),
});

export const RAIL = Object.freeze({
  chargeSeconds: 0.35,
  traceSeconds: 0.4,
  height: 0.2,
  inset: 0.04,
  radius: 0.006,
  strength: 1.4,
  push: 2.5,
  heft: 0.35,
  spray: 1.4,
  burst: 1.2,
  cue: 'railgun',
  force: 1,
  shock: Object.freeze({ trauma: 0.7, kick: 0.45, punch: 0.03, flash: 0.35 }),
});

export const CANNON = Object.freeze({
  radius: 0.03,
  surface: Object.freeze({ density: 3000, friction: 0.4, restitution: 0.25 }),
  speed: 6.5,
  inset: 0.07,
  reach: 0.05,
  reloadSeconds: 0.5,
  recoilSeconds: 0.25,
  capacity: 5,
  strength: [0.8, 3],
  impactSpeed: [2, 7],
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    hits: 5,
    falloff: 0.5,
    shatter: false,
    spray: 1.8,
    burst: 2,
    blast: Object.freeze({ reach: 0.2, speed: 3, heft: 0.3 }),
    cue: 'boom',
  }),
  landShock: Object.freeze({ trauma: 0.35, kick: 0.25, punch: 0.01, flash: 0 }),
  restSpeed: 0.05,
  restSeconds: 1,
  lifeSeconds: 8,
  fadeSeconds: 0.5,
});

export const ANVIL = Object.freeze({
  halfWidth: 0.065,
  halfHeight: 0.035,
  reach: 0.08,
  surface: Object.freeze({ density: 4200, friction: 0.7, restitution: 0.05 }),
  drop: 0.5,
  capacity: 3,
  strength: [1, 4],
  impactSpeed: [1.5, 6],
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    hits: 8,
    falloff: 0.4,
    shatter: false,
    spray: 2,
    burst: 1.6,
    blast: Object.freeze({ reach: 0.25, speed: 3.5, heft: 0.3 }),
    cue: 'anvil',
  }),
  landShock: Object.freeze({ trauma: 0.7, kick: 0.6, punch: 0.03, flash: 0 }),
  restSpeed: 0.05,
  restSeconds: 1.5,
  lifeSeconds: 10,
  fadeSeconds: 0.5,
});

export const SHOTGUN = Object.freeze({
  rate: 1.6,
  pellets: 9,
  spread: [0.055, 0.055],
  bloomSeconds: 1,
  recovery: 1,
  flashSeconds: 0.09,
  round: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.012,
    strength: 0.7,
    hits: 1,
    falloff: 0,
    shatter: false,
    spray: 0.6,
    burst: 1.2,
    contact: 0.004,
    blast: Object.freeze({ reach: 0.07, speed: 1.8, heft: 0.25 }),
    cue: 'shotgun',
    shock: Object.freeze({ trauma: 0.45, kick: 0.3, punch: 0.015, flash: 0.08 }),
  }),
});

export const JACKHAMMER = Object.freeze({
  rate: 16,
  stroke: 0.012,
  shake: 0.003,
  tension: 0.2,
  round: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.02,
    strength: 1.1,
    hits: 2,
    falloff: 0.3,
    shatter: false,
    spray: 1.2,
    burst: 1.1,
    contact: 0.006,
    blast: Object.freeze({ reach: 0.06, speed: 1.2, heft: 0.25 }),
    cue: 'jackhammer',
    shock: Object.freeze({ trauma: 0.2, kick: 0.04, punch: 0.003, flash: 0 }),
  }),
});

export const BOOMERANG = Object.freeze({
  capacity: 2,
  flightSeconds: 1.3,
  height: 0.25,
  inset: 0.05,
  bow: 0.35,
  size: 0.045,
  spin: 26,
  trail: 10,
  shock: Object.freeze({ trauma: 0.2, kick: 0.05, punch: 0.004, flash: 0.05 }),
});

export const ARROWS = Object.freeze({
  capacity: 36,
  volley: 9,
  spread: 0.14,
  slant: 0.25,
  speed: 4.5,
  lag: 0.25,
  reach: 0.008,
  size: 0.008,
  depth: 0.035,
  strength: 0.8,
  embed: 0.006,
  stickSeconds: 3,
  fadeSeconds: 0.4,
  blastSeconds: 0.3,
  shock: Object.freeze({ trauma: 0.12, kick: 0.04, punch: 0.002, flash: 0 }),
});

export const FIREWORK = Object.freeze({
  capacity: 3,
  speed: 3.2,
  wobble: 0.08,
  reach: 0.01,
  sparks: 40,
  sparkSpeed: [0.6, 2.2],
  sparkLife: [0.9, 1.5],
  sparkReach: 0.006,
  armSeconds: 0.1,
  sparkGravity: 0.35,
  sparkDrag: 1.2,
  blastSeconds: 0.35,
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.06,
    strength: 1.2,
    hits: 4,
    falloff: 0.5,
    shatter: false,
    spray: 1,
    burst: 1.5,
    contact: 0.01,
    blast: Object.freeze({ reach: 0.25, speed: 2.5, heft: 0.3 }),
    force: 0.35,
    cue: 'firework',
  }),
  ember: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.008,
    strength: 0.35,
    hits: 1,
    falloff: 0,
    shatter: false,
    spray: 0.4,
    burst: 0.5,
    contact: 0.004,
    blast: Object.freeze({ reach: 0.02, speed: 0.4, heft: 0.2 }),
    cue: 'crackle',
    shock: Object.freeze({ trauma: 0.05, kick: 0, punch: 0, flash: 0 }),
  }),
});

export const TSUNAMI = Object.freeze({
  speed: 3.2,
  strength: 1.3,
  spray: 12,
  height: 0.38,
  band: 0.22,
  grip: 10,
  buoyancy: 0.85,
  heft: 0.6,
  slamSeconds: 0.6,
  recedeSeconds: 0.6,
  splash: 2,
  surfSeconds: 0.15,
});

export const SONIC = Object.freeze({
  reach: 0.4,
  pulseSeconds: 0.1,
  rampSeconds: 2,
  hits: 3,
  strength: 0.55,
  fatigue: 0.06,
  glint: 0.3,
  rattle: 0.12,
  heft: 0.3,
  ringSeconds: 0.6,
  toneSeconds: 0.12,
  tension: 0.3,
});

export const CLUSTER = Object.freeze({
  capacity: 2,
  drop: 0.6,
  gravity: 3,
  reach: 0.02,
  size: 0.024,
  bomblets: 8,
  scatter: [0.8, 2],
  lift: 1.2,
  bombletReach: 0.008,
  armSeconds: 0.12,
  blastSeconds: 0.4,
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.075,
    strength: 2,
    hits: 5,
    falloff: 0.55,
    shatter: false,
    spray: 1.6,
    burst: 2,
    contact: 0.01,
    blast: Object.freeze({ reach: 0.25, speed: 4, heft: 0.3 }),
    force: 0.4,
    cue: 'explode',
  }),
});

export const MICROWAVE = Object.freeze({
  pulseSeconds: 0.12,
  rampSeconds: 1.5,
  spots: 3,
  strength: 0.6,
  fatigue: 0.05,
  char: 0.012,
  arcs: 0.6,
  humSeconds: 0.14,
  tension: 0.2,
});

export const HAIL = Object.freeze({
  rate: 18,
  capacity: 80,
  spread: 0.1,
  speed: 3.5,
  reach: 0.006,
  size: 0.006,
  blastSeconds: 0.1,
  round: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.01,
    strength: 0.5,
    hits: 1,
    falloff: 0,
    shatter: false,
    spray: 0.4,
    burst: 0.6,
    contact: 0.004,
    blast: Object.freeze({ reach: 0.03, speed: 0.6, heft: 0.2 }),
    cue: 'hail',
    shock: Object.freeze({ trauma: 0.06, kick: 0.01, punch: 0, flash: 0 }),
  }),
});

export const FIST = Object.freeze({
  windSeconds: 0.12,
  punchSeconds: 0.14,
  holdSeconds: 0.08,
  returnSeconds: 0.25,
  size: 0.07,
  shove: 5,
  lift: 1.2,
  reach: 0.3,
  heft: 0.35,
  blow: Object.freeze({
    radius: 0.09,
    strength: 3.2,
    hits: 8,
    falloff: 0.5,
    shatter: false,
    spray: 2,
    burst: 2.2,
    contact: 0.02,
    blast: Object.freeze({ reach: 0.3, speed: 3, heft: 0.35 }),
    force: 0.8,
    cue: 'punch',
  }),
});

export const PENDULUM = Object.freeze({
  radius: 0.075,
  surface: Object.freeze({ density: 1, friction: 0.4, restitution: 0.1 }),
  clearance: 0.03,
  reach: 1.3,
  damping: 0.25,
  settle: 0.12,
  follow: 1.6,
  cooldown: 0.25,
  contact: 1.15,
  strength: [0.8, 4],
  impactSpeed: [0.8, 5],
  tension: 0.25,
  blow: Object.freeze({
    hits: 8,
    falloff: 0.5,
    shatter: false,
    spray: 2,
    burst: 2.2,
    blast: Object.freeze({ reach: 0.25, speed: 3, heft: 0.35 }),
    cue: 'wrecking',
  }),
});

export const BOWLING = Object.freeze({
  radius: 0.045,
  surface: Object.freeze({ density: 2600, friction: 0.6, restitution: 0.1 }),
  speed: 7.5,
  spin: 60,
  inset: 0.06,
  capacity: 3,
  reach: 0.06,
  strength: [0.8, 3],
  impactSpeed: [1.5, 7],
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    hits: 5,
    falloff: 0.5,
    shatter: false,
    spray: 1.6,
    burst: 1.8,
    blast: Object.freeze({ reach: 0.2, speed: 3, heft: 0.3 }),
    cue: 'strikeout',
  }),
  landShock: Object.freeze({ trauma: 0.2, kick: 0.1, punch: 0, flash: 0 }),
  restSpeed: 0.05,
  restSeconds: 1,
  lifeSeconds: 6,
  fadeSeconds: 0.5,
});

export const PILEDRIVER = Object.freeze({
  halfWidth: 0.05,
  height: 0.16,
  rodWidth: 0.04,
  liftSeconds: 0.28,
  dropSeconds: 0.07,
  lift: 0.18,
  tension: 0.3,
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.07,
    strength: 2.6,
    hits: 6,
    falloff: 0.5,
    shatter: false,
    spray: 1.6,
    burst: 1.4,
    contact: 0.02,
    blast: Object.freeze({ reach: 0.2, speed: 2.5, heft: 0.3 }),
    force: 0.55,
    cue: 'piledrive',
  }),
});

export const STOMP = Object.freeze({
  dropSeconds: 0.14,
  holdSeconds: 0.22,
  liftSeconds: 0.3,
  size: 0.12,
  landShock: Object.freeze({ trauma: 0.9, kick: 0.7, punch: 0.04, flash: 0 }),
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.14,
    strength: 3.4,
    hits: 10,
    falloff: 0.45,
    shatter: false,
    spray: 2.2,
    burst: 2,
    contact: 0.03,
    blast: Object.freeze({ reach: 0.4, speed: 4.5, heft: 0.35 }),
    force: 0.85,
    cue: 'stomp',
  }),
});

export const MINE = Object.freeze({
  capacity: 6,
  radius: 0.022,
  armSeconds: 0.6,
  trigger: 0.035,
  blinkRate: 3,
  blastSeconds: 0.45,
  blow: Object.freeze({
    normalX: 0,
    normalY: -1,
    radius: 0.12,
    strength: 3,
    hits: 8,
    falloff: 0.55,
    shatter: true,
    spray: 2.2,
    burst: 2.6,
    contact: 0.02,
    blast: Object.freeze({ reach: 0.45, speed: 7, heft: 0.35 }),
    force: 0.8,
    cue: 'explode',
  }),
});

export const DISC = Object.freeze({
  capacity: 3,
  radius: 0.045,
  speed: 3.4,
  bounces: 3,
  spin: 40,
  inset: 0.05,
  height: 0.2,
  fall: 0.8,
  lifeSeconds: 4,
});

export const STORM = Object.freeze({
  strikeSeconds: 0.32,
  spread: 0.22,
  rain: 70,
  rainSpeed: 3,
  rainLength: 0.05,
  cloud: 0.18,
  darkness: 0.45,
});

export const VOLCANO = Object.freeze({
  eruptSeconds: 2.4,
  rate: 10,
  capacity: 60,
  speed: [1.6, 3.6],
  cone: 0.7,
  size: 0.012,
  reach: 0.01,
  arm: 0.08,
  crater: 0.07,
  embers: 6,
  smoke: 2,
  blastSeconds: 0.35,
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.045,
    strength: 1.1,
    hits: 3,
    falloff: 0.5,
    shatter: false,
    spray: 1.2,
    burst: 1.4,
    contact: 0.01,
    blast: Object.freeze({ reach: 0.15, speed: 2, heft: 0.3 }),
    force: 0.3,
    cue: 'boom',
  }),
});

export const SHURIKEN = Object.freeze({
  capacity: 12,
  volley: 3,
  fan: 0.14,
  speed: 5,
  spin: 30,
  inset: 0.05,
  height: 0.3,
  reach: 0.01,
  size: 0.014,
  depth: 0.03,
  strength: 0.9,
  embed: 0.004,
  stickSeconds: 2.5,
  fadeSeconds: 0.4,
  blastSeconds: 0.3,
  shock: Object.freeze({ trauma: 0.12, kick: 0.03, punch: 0.002, flash: 0 }),
});

export const REPULSOR = Object.freeze({
  growSeconds: 1.5,
  follow: 6,
  humSeconds: 0.15,
  flareSeconds: 0.4,
  reach: [0.15, 0.45],
  push: [3, 11],
  heft: 0.4,
  tension: [0.15, 0.5],
  burst: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.05,
    hits: 4,
    falloff: 0.5,
    shatter: false,
    spray: 1.2,
    burst: 2.4,
    contact: 0.01,
    cue: 'repulse',
  }),
  strength: [0.5, 2],
  speed: [4, 12],
});

export const BOMBER = Object.freeze({
  capacity: 2,
  speed: 2.2,
  altitude: 0.85,
  bombs: 6,
  spread: 0.5,
  reach: 0.012,
  size: 0.07,
  blastSeconds: 0.45,
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.11,
    strength: 2.6,
    hits: 7,
    falloff: 0.55,
    shatter: false,
    spray: 2,
    burst: 2.2,
    contact: 0.012,
    blast: Object.freeze({ reach: 0.35, speed: 5, heft: 0.35 }),
    force: 0.6,
    cue: 'explode',
  }),
});

export const GRENADE = Object.freeze({
  radius: 0.018,
  surface: Object.freeze({ density: 1800, friction: 0.9, restitution: 0.12 }),
  speed: 3.2,
  inset: 0.05,
  height: 0.3,
  fuseSeconds: 1.4,
  capacity: 4,
  impactSpeed: [1, 3],
  tumble: 8,
  blastSeconds: 0.45,
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.16,
    strength: 3.2,
    hits: 8,
    falloff: 0.6,
    shatter: true,
    spray: 2.2,
    burst: 2.6,
    contact: 0.045,
    blast: Object.freeze({ reach: 0.45, speed: 7, heft: 0.35 }),
    force: 0.85,
    cue: 'explode',
  }),
  restSpeed: 0.05,
  restSeconds: 60,
  lifeSeconds: 60,
  fadeSeconds: 0.01,
});

export const DRONE = Object.freeze({
  hover: 0.18,
  margin: 0.06,
  follow: 5,
  dive: 4.5,
  thrust: 14,
  reach: 0.02,
  size: 0.05,
  rotorSeconds: 0.12,
  blastSeconds: 0.45,
  tension: 0.2,
  blow: Object.freeze({
    normalX: 0,
    normalY: 1,
    radius: 0.14,
    strength: 3.2,
    hits: 8,
    falloff: 0.6,
    shatter: true,
    spray: 2.2,
    burst: 2.6,
    contact: 0.02,
    blast: Object.freeze({ reach: 0.5, speed: 7, heft: 0.35 }),
    force: 0.85,
    cue: 'explode',
  }),
});

export const WAVE = Object.freeze({
  chargeSeconds: 2,
  fireSeconds: 0.5,
  height: 0.25,
  inset: 0.05,
  radius: [0.012, 0.07],
  strength: [0.8, 3],
  push: [2, 7],
  heft: 0.4,
  spray: 2,
  burst: 1.8,
  cue: 'kame',
  shock: Object.freeze({ trauma: 0.9, kick: 0.5, punch: 0.04, flash: 0.45 }),
  tension: [0.1, 0.6],
});

export const IMPACT = Object.freeze({
  trauma: [0.25, 1],
  kick: [0.15, 1],
  punch: [0.008, 0.09],
  flash: [0, 0.55],
  hitStop: [0.02, 0.16],
});

export const FRAGMENTS = Object.freeze({
  minBodyCells: 24,
  maxBodies: 220,
  ccdCells: 1600,
  impactCooldown: 0.12,
  minImpactCells: 60,
  impactsPerFrame: 4,
  soundSpeed: 0.6,
  burstRadius: 0.13,
  burstSpeed: 1.3,
  burstLift: 0.45,
  burstSpin: 7,
});

export const SHOCKWAVE = Object.freeze({
  radius: 0.3,
  speed: 1.4,
  referenceMass: 0.5,
});

export const COMPLETION = Object.freeze({
  destruction: 0.99,
  settleDelay: 0.6,
  settleTimeout: 2.5,
});

export const RECORDING = Object.freeze({
  fps: 30,
  longEdge: 1280,
  outroSeconds: 3.6,
  busyGrace: 0.9,
  backlog: 8,
});
